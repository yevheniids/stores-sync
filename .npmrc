# Enable legacy peer dependencies to avoid conflicts
legacy-peer-deps=false

# Use exact versions for reproducible builds
save-exact=true

# Disable audit during install for faster builds
audit=false

# Fund messages can be disabled
fund=false
