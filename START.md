# 🚀 Инструкция по запуску проекта

## Предварительные требования

- ✅ Node.js 18+ (у вас: v22.0.0)
- ✅ PostgreSQL база данных (Supabase)
- ✅ Redis сервер
- ✅ Shopify Partner аккаунт

## Пошаговая инструкция

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

Убедитесь, что файл `.env` существует и содержит все необходимые переменные:

- `SHOPIFY_API_KEY` - API ключ из Shopify Partner Dashboard
- `SHOPIFY_API_SECRET` - API секрет
- `SHOPIFY_APP_URL` - URL приложения (https://localhost:3000)
- `DATABASE_URL` - строка подключения к PostgreSQL (Supabase)
- `REDIS_URL` - строка подключения к Redis
- `SESSION_SECRET` - секретный ключ для сессий

### 3. Настройка базы данных

```bash
# Генерация Prisma клиента
npm run prisma:generate

# Применение миграций
npm run prisma:migrate
```

### 4. Запуск Redis

```bash
# Запустите Redis сервер (если еще не запущен)
redis-server

# Или проверьте, что Redis запущен:
redis-cli ping
# Должно вернуть: PONG
```

### 5. Запуск проекта

#### Вариант 1: Автоматический запуск (рекомендуется)

```bash
# Сделайте скрипт исполняемым (если еще не сделано)
chmod +x start-dev.sh

# Запустите оба сервера одновременно
./start-dev.sh
```

Этот скрипт запустит:
- Shopify CLI (туннель для webhooks)
- Vite dev server (фронтенд + бэкенд)

#### Вариант 2: Ручной запуск (3 терминала)

**Терминал 1 - Shopify CLI:**
```bash
npm run dev
```

**Терминал 2 - Vite dev server:**
```bash
npm run dev:web
```

**Терминал 3 - Workers (обработка очередей):**
```bash
npm run workers:dev
```

### 6. Установка приложения в Shopify

⚠️ **ВАЖНО:** Не открывайте `https://localhost:3000` напрямую!

1. Откройте Shopify Admin:
   ```
   https://pros-cons-shop-test.myshopify.com/admin
   ```

2. Перейдите в раздел Apps:
   ```
   https://pros-cons-shop-test.myshopify.com/admin/apps
   ```

3. Найдите ваше приложение и нажмите **"Install app"** или **"Open app"**

4. Shopify автоматически:
   - Перенаправит на процесс установки
   - Запросит разрешения
   - Создаст сессию в базе данных
   - Перенаправит на Dashboard приложения

## Проверка работы

После запуска вы должны увидеть:

1. **Shopify CLI** - туннель активен, webhooks пересылаются
2. **Vite dev server** - доступен на `https://localhost:3000`
3. **Workers** - обрабатывают задачи из очередей
4. **Приложение в Shopify Admin** - Dashboard загружается без ошибок

## Логи

Просмотр логов (если используете `start-dev.sh`):

```bash
# Логи Shopify CLI
tail -f /tmp/shopify-cli.log

# Логи Vite сервера
tail -f /tmp/vite-server.log
```

## Остановка серверов

Если используете `start-dev.sh`:
- Нажмите `Ctrl+C` для остановки обоих серверов

Если запускали вручную:
- Остановите каждый процесс отдельно (`Ctrl+C` в каждом терминале)

## Устранение проблем

### Ошибка "localhost refused to connect"
- Убедитесь, что Vite сервер запущен
- Проверьте логи: `tail -f /tmp/vite-server.log`

### Ошибка 401 Unauthorized
- Убедитесь, что приложение установлено через Shopify Admin
- Проверьте, что сессия создана в базе данных

### Ошибка подключения к базе данных
- Проверьте `DATABASE_URL` в `.env`
- Убедитесь, что миграции применены: `npm run prisma:migrate`

### Ошибка подключения к Redis
- Убедитесь, что Redis запущен: `redis-cli ping`
- Проверьте `REDIS_URL` в `.env`

## Полезные команды

```bash
# Открыть Prisma Studio (GUI для базы данных)
npm run prisma:studio

# Проверка типов TypeScript
npm run typecheck

# Линтинг кода
npm run lint

# Форматирование кода
npm run format
```
